  	<% if current_user != item.user %>
    <% if current_user.following?(item.user) %>
    	<%= link_to 'フォロー外す', user_relationships_path(item.user.id), method: :delete, remote: true, class: "btn btn-danger" %>
    <% else %>
     	<%= link_to 'フォローする', user_relationships_path(item.user.id), method: :POST, remote: true, class: "btn btn-success"%>
    <% end %>
  	<% end %>