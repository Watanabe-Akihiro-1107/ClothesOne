
<div class="user_edit_main">
	<h4>ユーザー情報編集</h4>
	<%= form_for (@user) do |f|%>
		<%= f.text_field :nickname %>
		
		<%= f.label :profile_image %>
		<%= f.attachment_field :profile_image %>
		<%= f.submit "変更を保存"%>
	<%= link_to "退会する", delete_page_path %>
	<% end %>
</div>